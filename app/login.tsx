import { Link,router } from 'expo-router'; import { useState } from 'react'; import { KeyboardAvoidingView,Platform,StyleSheet,Text,TextInput,View } from 'react-native'; import { MaterialCommunityIcons } from '@expo/vector-icons'; import { AppButton } from '@/components/AppButton'; import { Screen } from '@/components/Screen'; import { COLORS } from '@/constants/colors'; import { signIn } from '@/lib/auth'; import { isConfigured } from '@/lib/supabase';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(){if(!email||!password){setError('Enter your email and password.');return}setBusy(true);setError('');const{error}=await signIn(email,password);setBusy(false);if(error)setError(error.message);else router.replace('/(tabs)')}return <Screen><KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={s.wrap}><View style={s.mark}><MaterialCommunityIcons name="qrcode-scan" size={34} color={COLORS.textOnPrimary}/></View><Text style={s.eyebrow}>QR ATTENDANCE</Text><Text style={s.title}>Welcome back</Text><Text style={s.sub}>Sign in to check in, manage events, and view attendance.</Text>{!isConfigured&&<View style={s.notice}><Text style={s.noticeTitle}>Connect Supabase first</Text><Text style={s.noticeText}>Copy .env.example to .env and add your project credentials.</Text></View>}<View style={s.form}><Text style={s.label}>Email address</Text><TextInput style={s.input} value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="you@school.edu" placeholderTextColor="#A38F93"/><Text style={s.label}>Password</Text><TextInput style={s.input} value={password} onChangeText={setPassword} secureTextEntry placeholder="At least 6 characters" placeholderTextColor="#A38F93"/>{error?<Text style={s.error}>{error}</Text>:null}<AppButton title={busy?'Signing in…':'Sign in'} icon="login" onPress={submit} disabled={busy}/></View><Text style={s.footer}>New here? <Link href="/register" style={s.link}>Create an account</Link></Text></KeyboardAvoidingView></Screen>}
const s=StyleSheet.create({wrap:{flex:1,justifyContent:'center',maxWidth:520,width:'100%',alignSelf:'center'},mark:{width:64,height:64,borderRadius:18,backgroundColor:COLORS.primary,alignItems:'center',justifyContent:'center',marginBottom:22},eyebrow:{color:COLORS.primary,fontSize:12,fontWeight:'800',letterSpacing:1.8,marginBottom:8},title:{fontSize:34,lineHeight:40,fontWeight:'800',color:COLORS.textPrimary},sub:{fontSize:16,lineHeight:24,color:COLORS.textSecondary,marginTop:10,marginBottom:28},form:{gap:10},label:{fontSize:14,fontWeight:'700',color:COLORS.textPrimary,marginTop:4},input:{height:54,borderWidth:1,borderColor:COLORS.border,borderRadius:12,backgroundColor:COLORS.card,paddingHorizontal:16,fontSize:16,color:COLORS.textPrimary,marginBottom:6},error:{color:COLORS.danger,lineHeight:20,marginBottom:4},footer:{textAlign:'center',color:COLORS.textSecondary,marginTop:24},link:{color:COLORS.primary,fontWeight:'800'},notice:{backgroundColor:COLORS.warningSoft,borderColor:'#EACD9E',borderWidth:1,borderRadius:12,padding:14,marginBottom:20},noticeTitle:{fontWeight:'800',color:COLORS.textPrimary},noticeText:{color:COLORS.textSecondary,marginTop:4,lineHeight:19}});
