import { MaterialCommunityIcons } from '@expo/vector-icons'; import { Pressable,StyleSheet,Text } from 'react-native'; import { COLORS } from '@/constants/colors';
export function AppButton({title,icon,onPress,variant='primary',disabled=false}:{title:string;icon?:keyof typeof MaterialCommunityIcons.glyphMap;onPress:()=>void;variant?:'primary'|'secondary'|'danger';disabled?:boolean}){return <Pressable accessibilityRole="button" accessibilityLabel={title} disabled={disabled} onPress={onPress} style={({pressed})=>[s.base,s[variant],pressed&&s.pressed,disabled&&s.disabled]}>{icon&&<MaterialCommunityIcons name={icon} size={20} color={variant==='secondary'?COLORS.primary:COLORS.textOnPrimary}/>}<Text style={[s.label,variant==='secondary'&&s.secondaryLabel]}>{title}</Text></Pressable>}
const s=StyleSheet.create({base:{minHeight:52,borderRadius:12,flexDirection:'row',alignItems:'center',justifyContent:'center',gap:10,paddingHorizontal:18,borderWidth:1},primary:{backgroundColor:COLORS.primary,borderColor:COLORS.primary},secondary:{backgroundColor:COLORS.card,borderColor:COLORS.border},danger:{backgroundColor:COLORS.danger,borderColor:COLORS.danger},pressed:{opacity:.82},disabled:{opacity:.45},label:{color:COLORS.textOnPrimary,fontSize:16,fontWeight:'700'},secondaryLabel:{color:COLORS.primary}});
